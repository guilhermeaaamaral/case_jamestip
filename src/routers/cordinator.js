export const goToHome = (history) => {
    history.push ('/')
}

export const goToRegistrationProducts = (history) => {
    history.push ('/registration/products')
}

export const goToQueryProducts = (history) => {
    history.push('/query/products')
}
